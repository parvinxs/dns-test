# dns-test

> ابزار ساده برای تست DNS / سرعت DNS / نمایش IP — تست آنلاین DNS، سرعت اینترنت و استخراج یا کپی DNS / IP  

## 📄 درباره

`dns-test` یک پروژه HTML خالص است که یک رابط ساده برای:
- تست DNS (resolve, speed, responsiveness)،  
- نمایش IP عمومی کاربر،  
- و امکان کپی کردن DNS یا IP انتخاب‌شده  

را در مرورگر فراهم می‌کند.  
مناسب برای کسانی‌ست که می‌خواهند بدون نیاز به نصب نرم‌افزار، به سرعت DNS و وضعیت اینترنت‌شان را بررسی کنند.  

## 🚀 ویژگی‌ها

- HTML خالص — بدون نیاز به بک‌اند یا نصب خاص.  
- رابط کاربری ساده و قابل درک — مناسب برای استفاده سریع.  
- تست DNS و سرعت اینترنت + نمایش IP عمومی.  
- امکان کپی کردن DNS یا IP انتخاب‌شده — برای استفاده سریع در تنظیمات شبکه.  

## 📥 راه‌اندازی / استفاده

1. مخزن را کلون یا دانلود کن:  
   ```bash
   git clone https://github.com/xsparvin/dns-test.git